EXPERIMENT_NAME: 'test-simple-md4allDDa-nuscenes-60m'

LOAD:
  CHECKPOINT_PATH: '/mnt/code/md4all/checkpoints/md4allDDa_nuscenes.ckpt'

DATASET:
  AUGMENTATION:
    NORMALIZE:
      MODE: 'Image' # Use Image mode if daytime is not known a priori

MODEL:
  DEPTH:
    MAX_DEPTH: 60.0  # 修改最大深度为60米

EVALUATION:
  DEPTH:
    MAX_DEPTH: 60.0  # 同时修改评估时的最大深度

LOSS:
  PHOTOMETRIC:
    WEIGHT: 0.0
  SMOOTHNESS_WEIGHT: 0.0
  VELOCITY_WEIGHT: 0.0
  SUPERVISED:
    WEIGHT: 1.0